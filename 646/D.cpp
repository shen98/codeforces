#include <iostream>

using namespace std;

int main() {
    int t;
    cin >> t;
    while(t--) {
        int n, k;
        cin >> n >> k;
        
        for(int i = 0; i < k; ++i) {
            int c;
            cin >> c;
            for(int j = 0; j < c; ++j) {
                
            }
        }
    }
    return 0;
}：
